-- Hogs up the resources

function init()
end

function update(dt)
  -- for i = 1, 3 do
  --   world.spawnProjectile("invisibleprojectile", mcontroller.position(), nil, nil, nil, {onlyHitTerrain = true})
  -- end
end